import styled from 'styled-components'
import {useHistory} from 'react-router-dom'

const Page = styled.div``

const Cart = () => {

    // /renter/cart
    // allows the renter to see everything they are currently requesting to rent, but not things they are currently renting
    // allows the renter to remove items from cart
    // submit cart
    
    return(
        <Page>

        </Page>
    )
}

export default Cart