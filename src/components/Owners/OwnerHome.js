import styled from 'styled-components'
import {useHistory} from 'react-router-dom'

const Page = styled.div``

const OwnerHome = () => {

    // /owner/:id
    // owner's profile page
    // display all items that they are renting and what is currently rented by whom
    // allows the owner to create new items for rent
    // allows the owner to edit current items for rent
    // allows the owner to terminate a renter contract
    // allows the owner to logout
    
    return(
        <Page>

        </Page>
    )
}

export default OwnerHome